# OpenSpec демо: `pagination-spaces-page-response`

Фича (одна и та же для сравнения с Spec Kit):  
`GET /api/spaces` и `GET /api/admin/spaces` возвращают пагинацию через `PageResponse`, а для не-ADMIN применяется `page/size`.

Артефакты:
- `01-proposal-pagination-spaces.md`
- `02-design-pagination-spaces.md`
- `03-tasks-pagination-spaces.md`
- `04-acceptance-pagination-spaces.md`

